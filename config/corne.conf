# Improve Bluetooth stability/range for wireless split communication.
CONFIG_BT_CTLR_TX_PWR_PLUS_8=y
CONFIG_ZMK_BLE_EXPERIMENTAL_CONN=y

# Enable nice!view display support.
# nice-view-gem provides its own custom display widgets, so do not enable
# ZMK's stock battery/layer/output/WPM widgets separately.
CONFIG_ZMK_DISPLAY=y

# Fetch and expose the peripheral half battery level on wireless split builds.
CONFIG_ZMK_SPLIT_BLE_CENTRAL_BATTERY_LEVEL_FETCHING=y
CONFIG_ZMK_SPLIT_BLE_CENTRAL_BATTERY_LEVEL_PROXY=y

# Blank the display when idle to reduce power use.
CONFIG_ZMK_DISPLAY_BLANK_ON_IDLE=y
