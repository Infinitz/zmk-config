manifest:
  remotes:
    # Default ZMK firmware
    - name: zmkfirmware
      url-base: https://github.com/zmkfirmware
  projects:
    # Default ZMK firmware
    - name: zmk
      remote: zmkfirmware
      revision: main
      import: app/west.yml
  self:
    path: config
